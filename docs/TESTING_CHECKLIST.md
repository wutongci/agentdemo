# 测试验证清单

> 更新日期: 2025-11-11
> 版本: v0.3-beta

## ✅ 编译测试

### 后端编译
```bash
cd backend
go build
```
**状态**: ✅ 通过 - 无错误

### 前端类型检查
```bash
cd frontend
npm run build
```
**状态**: ✅ 通过 - 成功构建

**已修复问题**:
- ✅ 添加 TypeScript 路径别名配置 (`tsconfig.app.json`)
- ✅ 添加 Vite 路径别名配置 (`vite.config.ts`)
- ✅ `@/lib/utils` 导入路径已解决
- ✅ 安装 `@radix-ui/react-tabs` 依赖
- ✅ 修复 `apiClient` 导入问题（改用 fetch API）
- ✅ 移除未使用的 `Button` 导入

---

## 🧪 功能测试清单

### 1. Middleware 控制台页面 🔧

#### 测试步骤
1. 启动后端: `cd backend && go run main.go`
2. 启动前端: `cd frontend && npm run dev`
3. 访问: `http://localhost:5173`
4. 点击 "🔧 Middleware" 按钮

#### 预期结果
- [ ] 页面正常加载，无错误
- [ ] 显示 3 个 Middleware Tab（summarization, filesystem, subagent）
- [ ] 每个 Tab 可以正常切换
- [ ] summarization 显示 "✅ 已启用" Badge
- [ ] filesystem 和 subagent 显示 "⏸️ 未启用" Badge
- [ ] filesystem Tab 显示 6 个工具（fs_read, fs_write, fs_ls, fs_edit, fs_glob, fs_grep）
- [ ] subagent Tab 显示 1 个工具（task）
- [ ] 功能说明卡片正常显示
- [ ] 底部提示卡片正常显示

#### API 测试
```bash
# 获取所有 Middleware
curl http://localhost:8080/api/middleware

# 获取 filesystem 工具列表
curl http://localhost:8080/api/middleware/filesystem/tools

# 获取 subagent 工具列表
curl http://localhost:8080/api/middleware/subagent/tools
```

---

### 2. 网络工具页面 🌐

#### 测试步骤
1. 确保后端和前端正在运行
2. 点击 "🌐 网络工具" 按钮

#### 预期结果
- [ ] 页面正常加载，无错误
- [ ] 显示 2 个 Tab（HTTP Request, Web Search）
- [ ] HTTP Request Tab 包含：
  - [ ] URL 输入框（默认值：GitHub API）
  - [ ] 方法选择器（6 个选项）
  - [ ] 请求头输入框（JSON 格式）
  - [ ] 请求体输入框（POST/PUT/PATCH 时显示）
  - [ ] "执行 HTTP 请求" 按钮
  - [ ] 响应显示区域
  - [ ] 使用示例卡片
- [ ] Web Search Tab 包含：
  - [ ] 查询输入框
  - [ ] 结果数量输入框（1-10）
  - [ ] 主题类型选择器（3 个选项）
  - [ ] "执行 Web 搜索" 按钮
  - [ ] 结果显示区域
  - [ ] 配置说明卡片
  - [ ] 使用示例卡片

#### 交互测试
- [ ] 点击 "执行 HTTP 请求" 按钮，显示演示信息
- [ ] 点击 "执行 Web 搜索" 按钮，显示演示信息
- [ ] 切换 HTTP 方法，请求体输入框正确显示/隐藏
- [ ] 修改参数后，界面正常响应

---

### 3. 现有页面兼容性测试

#### 协作工作流页面 🤝
- [ ] 页面正常加载
- [ ] 无 JavaScript 错误
- [ ] 功能正常工作

#### 简单对话页面 💬
- [ ] 页面正常加载
- [ ] 可以创建新会话
- [ ] 可以发送消息
- [ ] WebSocket 连接正常
- [ ] 消息正常显示

#### Skills 管理页面 ⚡
- [ ] 页面正常加载
- [ ] Commands 列表正常显示
- [ ] Skills 列表正常显示
- [ ] 功能正常工作

---

### 4. 导航测试

#### 页面切换
- [ ] 点击 "🤝 协作工作流" - 正确切换
- [ ] 点击 "💬 简单对话" - 正确切换
- [ ] 点击 "🔧 Middleware" - 正确切换
- [ ] 点击 "🌐 网络工具" - 正确切换
- [ ] 点击 "⚡ Skills" - 正确切换

#### 导航状态
- [ ] 当前页面按钮显示蓝色背景
- [ ] 其他页面按钮显示灰色背景
- [ ] 鼠标悬停时背景色正确变化

---

### 5. API 端点测试

#### Middleware API
```bash
# 1. 获取所有 Middleware
curl http://localhost:8080/api/middleware
# 预期: 返回 3 个 Middleware 的 JSON

# 2. 获取 Agent Middleware 配置
# 需要先创建会话获取 agentId
SESSION_ID=$(curl -s -X POST http://localhost:8080/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"title":"Test"}' | jq -r '.id')

curl http://localhost:8080/api/middleware/agent/$SESSION_ID
# 预期: 返回 ["summarization"]

# 3. 获取 Middleware 工具列表
curl http://localhost:8080/api/middleware/filesystem/tools
# 预期: 返回 6 个工具

curl http://localhost:8080/api/middleware/subagent/tools
# 预期: 返回 1 个工具（task）

curl http://localhost:8080/api/middleware/summarization/tools
# 预期: 返回 0 个工具
```

#### 现有 API
```bash
# Session API
curl -X POST http://localhost:8080/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Session"}'
# 预期: 返回会话 ID

curl http://localhost:8080/api/sessions
# 预期: 返回会话列表

# Skills API
curl http://localhost:8080/api/skills/commands
# 预期: 返回 Commands 列表

curl http://localhost:8080/api/skills/skills
# 预期: 返回 Skills 列表
```

---

### 6. 网络工具实际测试（可选）

#### HTTP Request 工具
**通过 AI 对话调用**:
1. 进入 "💬 简单对话" 页面
2. 发送消息: "请使用 http_request 工具访问 https://api.github.com/repos/golang/go"
3. 观察 AI 是否调用工具
4. 检查返回结果

**预期**:
- AI 识别到需要使用 http_request 工具
- 工具成功执行
- 返回 GitHub 仓库信息

#### Web Search 工具（需要 API Key）
**配置**:
```bash
export TAVILY_API_KEY="tvly-xxxxx"
# 重启后端
```

**测试**:
1. 进入 "💬 简单对话" 页面
2. 发送消息: "请使用 web_search 搜索 'AgentSDK features'"
3. 观察 AI 是否调用工具
4. 检查搜索结果

**预期**:
- AI 识别到需要使用 web_search 工具
- 工具成功调用 Tavily API
- 返回搜索结果（标题、URL、摘要）

---

### 7. Middleware 自动触发测试（高级）

#### Summarization Middleware
**测试方法**:
1. 进入 "💬 简单对话" 页面
2. 创建新会话
3. 发送多条长消息（模拟达到 170k tokens）
4. 观察后端日志

**预期后端日志**:
```
[Middleware] Summarization triggered: tokens=180000
[Middleware] Summarizing 20 old messages, keeping recent 6
[Middleware] Summary created, saved 150000 tokens
```

**注意**: 实际触发需要大量消息，可以通过修改阈值测试。

---

## 🔍 UI/UX 测试

### 响应式设计
- [ ] 在不同屏幕尺寸下测试（桌面、平板、手机）
- [ ] Tab 切换动画流畅
- [ ] 卡片布局正确

### 视觉一致性
- [ ] Badge 颜色正确（active: 蓝色, inactive: 灰色）
- [ ] 按钮样式一致
- [ ] 字体大小和间距合理

### 交互反馈
- [ ] 按钮点击有视觉反馈
- [ ] 加载状态显示正确
- [ ] 错误提示友好

---

## 📋 浏览器兼容性测试

### Chrome
- [ ] 页面正常显示
- [ ] 所有功能正常

### Firefox
- [ ] 页面正常显示
- [ ] 所有功能正常

### Safari
- [ ] 页面正常显示
- [ ] 所有功能正常

### Edge
- [ ] 页面正常显示
- [ ] 所有功能正常

---

## 🐛 已知问题

### 已修复
- ✅ TypeScript 路径别名问题 - 已添加配置
- ✅ Vite 导入解析问题 - 已添加 alias 配置

### 待修复
- 无

---

## 📝 测试记录

### 测试日期: 2025-11-11

#### 编译测试
- [x] 后端编译: ✅ 通过
- [ ] 前端构建: 待测试

#### 功能测试
- [ ] Middleware 控制台: 待测试
- [ ] 网络工具页面: 待测试
- [ ] 页面切换: 待测试
- [ ] API 端点: 待测试

#### 测试人员
- 待测试

#### 测试环境
- OS: macOS
- Node.js: v18+
- Go: v1.21+
- 浏览器: Chrome/Firefox/Safari

---

## 🚀 快速测试命令

### 完整测试流程
```bash
# 1. 安装依赖
cd frontend && npm install

# 2. 编译测试
cd ../backend && go build
cd ../frontend && npm run build

# 3. 启动服务
# Terminal 1
cd backend && go run main.go

# Terminal 2
cd frontend && npm run dev

# 4. 访问测试
open http://localhost:5173

# 5. API 测试
curl http://localhost:8080/api/middleware
curl http://localhost:8080/api/middleware/filesystem/tools
```

### 快速烟雾测试（3 分钟）
1. 启动后端和前端
2. 访问每个页面，确保无错误
3. 点击 Middleware 控制台，切换 Tab
4. 点击网络工具，查看界面
5. 测试一个 API 端点

---

## ✅ 测试完成标准

### 必须通过
- [x] 后端编译成功
- [ ] 前端构建成功
- [ ] 所有 5 个页面可以访问
- [ ] Middleware API 返回正确数据
- [ ] UI 显示正常，无明显错误

### 推荐通过
- [ ] 所有 API 端点测试通过
- [ ] 浏览器兼容性良好
- [ ] 响应式设计正常
- [ ] 实际工具调用成功（HTTP Request）

### 可选测试
- [ ] Web Search 工具（需要 API Key）
- [ ] Summarization 自动触发
- [ ] 性能测试
- [ ] 压力测试

---

**测试完成后，请更新此文档记录测试结果！**
